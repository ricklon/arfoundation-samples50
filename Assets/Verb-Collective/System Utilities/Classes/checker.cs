/*
 * This is a basic script that creates a boolean
 * the boolean is used by the hasLanded script
 * to check and see if a game object has landed 
 * on the ground.
 * 
 * This very basic combination can be customized
 * to quickly generate custom qualities or settings 
 * for a variety of objects
 */

using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class checker : MonoBehaviour
{
    //This declaration is where the Boolean is created
    public bool isGround = true;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
